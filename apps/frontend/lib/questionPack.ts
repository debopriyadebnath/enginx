/**
 * Types for hardcoded question JSON (`meta` + `questions`).
 * Runtime seeding uses `convex/seed/questionPack.ts` + `convex/seed/*.json`.
 */
export type {
  QuestionPackMeta,
  QuestionJson,
  QuestionPackFile,
  QuestionPackSource,
} from "../convex/seed/questionPack";
